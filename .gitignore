node_modules/
dist/
*.js.map
*.d.ts.map
plans/
.plan-build/
.svelte-kit/
.preview-cache/
.community-tags-cache/
.plan-cache.json
.plan-history-cache.json
.claude/*
!.claude/settings.json
.gallery/

# Transient per-test scratch directories. Each test creates one and removes it
# in `afterEach`, so they only exist mid-run — but a `git status` during a suite
# sees them, which is enough to trip tooling that watches for untracked files.
.tmp-*/
