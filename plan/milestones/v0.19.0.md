{% milestone name="v0.19.0" status="planning" %}

# v0.19.0 — Polish, composability & rollups

Where v0.18.0 hardened the rune output *contract*, v0.19.0 builds on that stable
base along three threads: visual polish of the Lumina theme, making rune
*composability* a supported and validated surface ({% ref "SPEC-084" /%}), and
improving the cross-page *aggregation* runes that the plan plugin leans on.

## Lumina visual polish

- {% ref "WORK-340" /%} — token discipline: raw hex → `--rf-color-*`
- {% ref "WORK-341" /%} — dark-mode token parity audit

## Rune composability ({% ref "SPEC-084" /%})

- {% ref "WORK-336" /%} — audit and rationalize context modifiers (drop Hero `in-feature`)
- {% ref "WORK-337" /%} — enforce parent nesting at build time
- {% ref "WORK-338" /%} — composability authoring contract + CLI coverage audit
- {% ref "WORK-339" /%} — media-zone guest adaptation (any visual rune sits cleanly in a media zone)
- {% ref "WORK-345" /%} — bento cell media zone via explicit `---` split
- {% ref "WORK-346" /%} — rune compositions docs category (Family A first)

## Aggregation rollups

- {% ref "WORK-296" /%} — decompose plan-progress as sugar over `aggregate` (incl. the achieved-status union default + milestone scope)
- {% ref "WORK-342" /%} — modernize plan card-sugars to compose with the `bar` rune
- {% ref "WORK-344" /%} — expose group metadata in `collection` item templates

Open-world achievement ({% ref "WORK-343" /%} — schema-declared `achievedStatus`)
is deferred beyond v0.19.0; the C union default in {% ref "WORK-296" /%} covers
every first-party type today.

## Also shipping

- {% ref "WORK-293" /%} — html-scaffold client-behaviors bundle (carried from v0.18.0)

{% /milestone %}
