{% milestone name="v0.17.0" status="planning" %}

# v0.17.0 — Polish, composition, and reference primitives

The v0.17.0 milestone follows v0.16.0's registry-driven sites by finishing
deferred polish, lifting the patterns the plan-site scaffold surfaced into
proper runes, and broadening the reference vocabulary so docs can point
inline at files and types without leaving the page.

## Goals

- Decide the future of `plan-progress` now that the canonical
  dashboard pattern is markdoc composition over `aggregate` ({% ref "WORK-296" /%}).
- Ship the html-scaffold client-behaviors bundle so the no-build adapter
  reaches behavioral parity with the framework adapters ({% ref "WORK-293" /%}).
- Land the `file-ref` rune + shared `preview="…"` attribute on `xref`,
  with single-block drawer styling, per {% ref "SPEC-078" /%} — once the
  docs surface needs it.
- Author-registered composition runes per {% ref "SPEC-077" /%} — held
  until real demand surfaces from the docs-overhaul work.

## Work

- {% ref "WORK-293" /%} — html scaffold client behaviors bundle (SPEC-073 follow-up)
- {% ref "WORK-296" /%} — plan-progress decomposition (or deprecation) over aggregate

Additional work items will be filed against this milestone as SPEC-077 /
SPEC-078 / the theme-tokens reference work moves from draft to scoped.

{% /milestone %}
