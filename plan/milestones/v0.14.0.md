{% milestone name="v0.14.0" status="complete" %}

# v0.14.0 — Design tokens contract + brand identity

The v0.14.0 milestone establishes refrakt's design tokens contract as a typed, theme-agnostic surface ({% ref "SPEC-048" /%}), refreshes the brand mark with a monochrome prism logo ({% ref "SPEC-050" /%}), aligns tint vocabulary with the rest of the token system ({% ref "SPEC-053" /%}), ships Lumina with a neutral default palette plus two opt-in presets — `tideline` and `niwaki` — that demonstrate the preset architecture ({% ref "SPEC-051" /%}), and lets authors lock or unlock tint modes per page or subtree via frontmatter cascade ({% ref "SPEC-052" /%}).

Together these are the foundational pieces that v1.0 needs in place: a coherent token surface, a brand identity that doesn't compete with the user's, and the preset / cascade mechanics that make Lumina genuinely re-skinnable.

## Implementation order

Specs ship in dependency order rather than numeric order:

1. **{% ref "SPEC-048" /%}** — typed token contract, preset merge, dark-mode-as-overlay. Foundation; everything else depends on it.
2. **{% ref "SPEC-050" /%}** (independent, parallel-able) and **{% ref "SPEC-053" /%}** (tint vocabulary cleanup; lands before SPEC-051 ships tint code in the new vocab).
3. **{% ref "SPEC-051" /%}** — neutral default palette, syntax + status palettes, tideline preset, niwaki preset, fonts, docs pages.
4. **{% ref "SPEC-052" /%}** — per-page and per-subtree tint cascade; refrakt site adopts dark-locked default with auto docs.

## Work items

### {% ref "SPEC-048" /%} — Design tokens contract (foundation)

- {% ref "WORK-185" /%} — Typed token contract in `@refrakt-md/types`
- {% ref "WORK-186" /%} — Highlighter token rename (`--shiki-*` → `--rf-syntax-*`)
- {% ref "WORK-187" /%} — Config-driven token stylesheet generation
- {% ref "WORK-188" /%} — Dark mode as `PartialTokenContract` overlay
- {% ref "WORK-189" /%} — `theme.colorScheme` initial state field
- {% ref "WORK-190" /%} — Preset loading and merge order
- {% ref "WORK-191" /%} — Migrate existing Lumina tokens to the new contract

### {% ref "SPEC-050" /%} — Prism logo

- {% ref "WORK-192" /%} — Author the canonical prism SVG asset
- {% ref "WORK-193" /%} — Favicon and app icon set
- {% ref "WORK-194" /%} — Replace brand mark on the refrakt site

### {% ref "SPEC-053" /%} — Tint shape alignment

- {% ref "WORK-195" /%} — Update `TintTokens` / `TintDefinition` types
- {% ref "WORK-196" /%} — `mergeThemeConfig` extends support + remove deprecated `RefraktConfig.tints`
- {% ref "WORK-197" /%} — Engine + `tint.css` rename to new `--tint-*` property names
- {% ref "WORK-198" /%} — Migrate Lumina and plugin tint configs
- {% ref "WORK-199" /%} — Document the migration recipe

### {% ref "SPEC-051" /%} — Lumina neutral default + presets

- {% ref "WORK-200" /%} — Neutral default body palette (light + dark)
- {% ref "WORK-201" /%} — Syntax highlighting palette (5 hues + comment + punctuation)
- {% ref "WORK-202" /%} — Status palette (4 sentiments × 3 tokens × 2 modes)
- {% ref "WORK-203" /%} — Google Fonts loading (Inter, JetBrains Mono, IBM Plex Sans, IBM Plex Mono)
- {% ref "WORK-204" /%} — Tideline preset module (current palette + Plex typography)
- {% ref "WORK-205" /%} — Niwaki preset module (syntax-only)
- {% ref "WORK-206" /%} — Refrakt site adopts `presets: ["niwaki"]`
- {% ref "WORK-207" /%} — `create-refrakt` template + post-init preset surfaces
- {% ref "WORK-208" /%} — Preset documentation pages with design-plugin runes
- {% ref "WORK-209" /%} — Site screenshots refreshed against new appearance
- {% ref "WORK-210" /%} — v1.0 migration note for existing sites

### {% ref "SPEC-052" /%} — Tint cascade

- {% ref "WORK-211" /%} — Add theme toggle to Lumina (prerequisite — none exists today)
- {% ref "WORK-212" /%} — Cascade resolution function in `@refrakt-md/content`
- {% ref "WORK-213" /%} — Frontmatter schema extension (`tint`, `tint-mode`, `tint-lock`)
- {% ref "WORK-214" /%} — Renderer integration + pre-paint script
- {% ref "WORK-215" /%} — Refrakt site adopts the cascade (root locked dark; docs/runes/plan auto)
- {% ref "WORK-216" /%} — `/docs/themes/tint-cascade` documentation page

## Out of milestone (deferred)

- Wordmark companion to the prism logo — separate work, no v0.14.0 spec for it
- Additional Lumina presets beyond tideline and niwaki (`midnight`, `slate`, etc.) — community contribution territory
- Animated tint transitions, user-facing tint picker UI, font-serif slot — explicitly out of scope of the contributing specs

{% /milestone %}
