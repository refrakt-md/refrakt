{% milestone name="v0.14.3" status="active" %}

# v0.14.3 — Navigation enrichment: slug resolution and richer dropdowns

The v0.14.3 milestone delivers two coordinated navigation specs: {% ref "SPEC-055" /%} fixes nav slug ambiguity and the multiple-items-active-at-once symptom by moving slug resolution to build time and introducing multi-segment slugs as the first-class disambiguator; {% ref "SPEC-054" /%} extends the existing nav layouts so authors can compose Linear- / Vercel- / Stripe-style header dropdowns from the primitives they already know — `layout="menubar"` accepts arbitrary block content inside `## groups` with a position-based intro / footer slot rule, `layout="columns"` gains a `---`-between-sections column-flow rule plus a headingless mode, and a new `layout="strip"` lands for compact secondary link rows. Plus a new core `{% badge %}` inline rune and a generalisation of `auto=true` description / icon enrichment to every layout.

SPEC-055 is foundational — it has to land first because the menubar enrichment is much cleaner once URL resolution is build-time and unambiguous. SPEC-054 layers on top, reusing the slug resolution path and the existing `auto=true` frontmatter enrichment machinery.

A patch milestone scoped to navigation polish. Builds on {% ref "SPEC-046" /%} (which defined the original `layout` attribute and deferred mega-style menus + per-item enrichment to future work).

## Implementation order

SPEC-055 work items land first as the foundation. SPEC-054 items can begin once description resolution generalises, but the menubar enrichment depends on SPEC-055 being merged.

1. **{% ref "WORK-231" /%}** — Build-time slug resolution + error system (the foundational refactor; everything else assumes resolved hrefs in SSR HTML)
2. **{% ref "WORK-232" /%}** — Build-time active state + Lumina CSS (depends on WORK-231 for resolved hrefs)
3. **{% ref "WORK-233" /%}** — Site nav migration + authoring docs (verifies existing navs still work, documents the new rules)
4. **{% ref "WORK-234" /%}** — `{% badge %}` core inline rune (independent — can ship in parallel with anything above)
5. **{% ref "WORK-235" /%}** — Description / icon enrichment generalised across `auto=true` layouts
6. **{% ref "WORK-236" /%}** — Menubar enrichment, columns flow, and strip layout: engine + Lumina CSS + behavior extension (depends on WORK-234 + WORK-235)
7. **{% ref "WORK-237" /%}** — Rich-dropdown authoring docs + site demo (lands last as the editorial pass)
8. **{% ref "WORK-238" /%}** — Docs IA split: separate author handbook from developer ("extend") handbook (coordinated editorial pass landing alongside the nav work)

## Work items

### {% ref "SPEC-055" /%} — Nav slug resolution and active state

- {% ref "WORK-231" /%} — Build-time slug resolution + error system
- {% ref "WORK-232" /%} — Build-time active state + Lumina CSS
- {% ref "WORK-233" /%} — Site nav migration + authoring docs

### {% ref "SPEC-054" /%} — Nav richer dropdowns and column flow

- {% ref "WORK-234" /%} — `{% badge %}` core inline rune
- {% ref "WORK-235" /%} — Description / icon enrichment generalised across auto layouts
- {% ref "WORK-236" /%} — Menubar enrichment, columns flow, and strip layout (engine + CSS + behavior)
- {% ref "WORK-237" /%} — Rich-dropdown authoring docs + site demo

### Docs information architecture

- {% ref "WORK-238" /%} — Docs IA split into author handbook and developer ("extend") handbook

## Out of milestone (deferred)

- **`layout="mega"` as a dedicated layout value** — explicitly *not* introduced. {% ref "SPEC-054" /%} absorbs that responsibility into menubar via composition (menubar groups accept any block content; nested `columns` navs provide multi-column panel content). If a real-world case proves the composition model insufficient, revisit.
- **`base="/path"` attribute on `{% nav %}`** — {% ref "SPEC-055" /%} explicitly defers this until a use case appears. Nav-location-relative resolution plus multi-segment slugs cover the realistic patterns; adding the attribute is YAGNI today.
- **`nav-key` frontmatter** — {% ref "SPEC-055" /%} rejects a second identifier system. If renaming-heavy or very deep URL paths force the issue later, revisit in a separate spec.
- **Embedded media in panel slots beyond plain images and nested runes** — video, animated demos, embedded iframes. Plain Markdown `![image]` and nested `{% nav %}` runes work in intro / footer slots; richer media deferred.
- **Deeper nesting in panels** — sub-sub-sections (multiple levels of `##` inside a single panel). One level of nesting (group → nested nav with its own sections) covers realistic patterns; deeper nesting is YAGNI.
- **Aggregated / auto-populated panel slots** — e.g. last 3 changelog entries auto-pulled into a panel footer. Slot content is hand-authored in v1; a future `{% nav-recent %}` primitive could populate it.
- **Strip layout with groups** — `layout="strip"` is flat by design and warns on `##` headings. Grouped strip-like content should use `layout="columns"` or `layout="vertical"`.
- **Migration tool for ambiguous slugs** — {% ref "SPEC-055" /%} deliberately ships the build error as the migration tool. A custom CLI command is unnecessary ceremony.
- **Custom active-state matching rules** — exact + longest-prefix covers every realistic case. Regex / glob matching is over-engineering for a nav primitive.
- **A new declarative content-model primitive (`delimitedSections`)** — investigated in conversation; deferred. Nav's `headingsToList()` imperative path is already in place and well-understood. Adding a framework primitive for one consumer is premature; revisit if a second rune needs the same shape.

{% /milestone %}
