{% milestone name="v0.20.1" status="active" %}

# v0.20.1 — Surface model: docs & showcase

A documentation-and-showcase patch on top of v0.20.0. The surface model shipped
with per-rune reference docs; this milestone makes it *land* — a cohesive
showcase of the vocabulary as a system — and finishes the deferred
composition/showcase work from v0.19.0, now that both the bento substrate and the
surface model exist to feed it. No new runtime features.

## Showcase the surface model

- {% ref "WORK-380" /%} — a dedicated surface-model gallery page: chrome, fills,
  cover layout, and posture demonstrated together as one curated page (SPEC-086–090).

## Finish the composition/showcase work (carried from v0.19.0, enriched)

- {% ref "WORK-346" /%} — the **Compositions** docs category (page-per-pattern),
  now including the surface compositions (cover poster, framed media, substrate /
  tinted / gradient surfaces) alongside the Family-A media-zone guests.
- {% ref "WORK-350" /%} — rebuild the index composability section as a live
  **bento showcase** — the capstone that *is* a composition — now able to flex
  cover cards, framed media, and tinted / substrate cells.

## Also shipping (carried from v0.19.0)

- {% ref "WORK-293" /%} — ship the client-behaviors bundle in the `@refrakt-md/html`
  scaffold so scaffolded static sites get working interactive runes (carried from
  v0.18.0). Unrelated to the surface model, but the last open non-doc item — folded
  in to clear the backlog.

{% /milestone %}
