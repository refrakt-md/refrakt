{
  "name": "refract-md",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "themes/*",
    "site"
  ],
  "scripts": {
    "build": "npm run build -ws",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^25.2.3",
    "typescript": "^5.4.0",
    "vitest": "^2.0.0"
  }
}
