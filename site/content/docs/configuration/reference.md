---
title: Configuration reference
description: Every field a site accepts in refrakt.config.json — generated from the schema, so it cannot fall behind the code
---

# Configuration reference

Every field a **site** accepts in `refrakt.config.json`, grouped by what it does.

This page is generated from `refrakt.config.schema.json` — the same schema your editor validates against — so it lists what the code actually accepts rather than what someone remembered to write down. For how the file is *shaped* (single-site, multi-site, path resolution), see [Sites](/docs/configuration/sites).

{% hint type="note" %}
Fields marked **theme-defaultable** can also be supplied by a theme's manifest. The site config wins; the theme is the fallback.
{% /hint %}

## Content and theme

What the site is built from.

{% data src="site/content/_data/config-fields.json" root="fields" where="group:content-theme" %}
### {% $row.name %}

**{% $row.type %}**{% if $row.required %} · required{% /if %}

{% $row.description %}
{% /data %}

## Site identity

Used for canonical links, social cards and structured data. Every field here is theme-defaultable.

{% data src="site/content/_data/config-fields.json" root="fields" where="group:site-identity" %}
### {% $row.name %}

**{% $row.type %}**

{% $row.description %}
{% /data %}

## Routing and entities

How pages map to layouts, and which of them become registry entities.

{% data src="site/content/_data/config-fields.json" root="fields" where="group:routing" %}
### {% $row.name %}

**{% $row.type %}**

{% $row.description %}
{% /data %}

## Runes and plugins

Which runes exist, and which package wins when names collide. See [Plugins](/docs/configuration/plugins) for the conflict-resolution knobs in full.

{% data src="site/content/_data/config-fields.json" root="fields" where="group:runes-plugins" %}
### {% $row.name %}

**{% $row.type %}**

{% $row.description %}
{% /data %}

## Presentation

Cross-cutting visual configuration.

{% data src="site/content/_data/config-fields.json" root="fields" where="group:presentation" %}
### {% $row.name %}

**{% $row.type %}**

{% $row.description %}
{% /data %}

## Content sources

Where source links resolve from.

{% data src="site/content/_data/config-fields.json" root="fields" where="group:content-sources" %}
### {% $row.name %}

**{% $row.type %}**

{% $row.description %}
{% /data %}

## Internationalisation

Locale selection and string overrides — see [i18n](/docs/configuration/i18n) for the resolution order.

{% data src="site/content/_data/config-fields.json" root="fields" where="group:i18n" %}
### {% $row.name %}

**{% $row.type %}**

{% $row.description %}
{% /data %}

## Keeping this page honest

The rows above come from `site/content/_data/config-fields.json`, generated by `scripts/generate-config-reference.mjs` from the schema. A colocated test fails when the artifact is stale, when a schema field has no group, or when a group entry names a field that no longer exists — so a field added to the config cannot quietly go undocumented.

Regenerate with:

```shell
npm run config:reference
```

## See also

- [Sites](/docs/configuration/sites) — single-site vs multi-site, and path resolution
- [Plugins](/docs/configuration/plugins) — installing plugins and resolving rune conflicts
- [Schema](/docs/configuration/schema) — using the published JSON Schema in your editor
